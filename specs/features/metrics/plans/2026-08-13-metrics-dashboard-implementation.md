# 量化看板实现计划（2026-08-13）

## 目标

在本地 SQLite 原型内交付一级“量化看板”，完整覆盖九域 25 项核心 KPI，并以差异化页面、33 能力项覆盖、历史快照和改进事项实现量化管理闭环。

## 实现顺序

1. 建立 `types.ts`、`catalog.ts`、`default-state.ts`、`store.tsx`。
2. 建立 `MetricsLayout`、九域 Tab 和九个独立子路由。
3. 实现共享 KPI、趋势、能力覆盖、风险与弹窗组件。
4. 实现九个专属业务视图。
5. 接入菜单和 `src/app/router.tsx`。
6. 同步平台文档并执行工程与视觉验证。

## 约束

- 不覆盖用户在工作区内的其他未提交改动。
- 不直接 import 其他 feature；跨域数据以 mock 事实和稳定证据 ID 表达。
- 不新增真实外部连接、生产调度或认证结论。
- 不新增测试产品模块；工程验证仍使用现有命令。

