# Knowledge Center · Tasks

## 文档自定义解析设置
- [x] 为文档 mock 模型增加自动/自定义模式与自定义解析配置。
- [x] 将行内解析器下拉框替换为居中解析设置弹窗和解析方式单选框。
- [x] 增加分片大小、分片重叠校验及保存回显。
- [x] 增加文档预处理总开关、规则说明和敏感内容处理策略。
- [x] 保留上次有效自定义配置, 并让新上传文档默认自动解析。
- [x] 运行 `npm run typecheck`、`npm run lint`、`npm run build`。
- [ ] 完成浏览器交互验证（当前本地地址被浏览器安全策略阻止）。

## 解析策略提示改造
- [x] 新增解析策略配置, 统一维护展示名和适合文档格式。
- [x] 将新建/编辑弹窗“默认解析器”改为“解析策略”。
- [x] 将下拉选项改为 9 种解析策略。
- [x] 在解析策略字段下方展示当前策略适合文档格式。
- [x] 默认策略改为“自动解析（推荐）”。
- [x] 将知识库列表列头“解析器”同步改为“解析策略”。
- [x] 更新 mock 数据中的 `naive 通用文档` 为“通用文档”, 并保留旧值映射。
- [x] 运行 `npm run build` 验证构建通过。

## 召回测试页面改造
- [x] 将本计划同步写入 knowledge-center specs。
- [x] 删除知识库详情页旧“召回测试”按钮、弹窗 state、`RecallTestDialog` 和相关类型/import。
- [x] 新增 `RecallTestPage.tsx`, 包含测试设置、召回结果、召回链路、测试历史。
- [x] 将召回测试页保持为左侧设置/历史 + 右侧结果/链路的两栏工作台布局。
- [x] 在知识库列表页顶部新增“召回测试”按钮, 跳转 `/knowledge-center/knowledge-bases/recall-test`。
- [x] 在 `routes.tsx` 注册 `knowledge-bases/recall-test`, 并放在动态详情路由前。
- [x] 新增 `POST /api/knowledge-center/recall-test` mock 接口, 复用 `vectorRecords` 支持多知识库过滤。
- [x] 为向量检索、关键字检索、图谱检索、混合检索返回不同的详细召回链路。
- [x] 运行 `npm run build` 验证构建通过。
- [ ] 启动本地页面并验证列表入口、详情页入口移除、4 种链路切换和历史回填。

## 知识向量入口与列表字段
- [x] 将本轮知识向量入口、返回行为和列表字段同步写入 knowledge-center specs。
- [x] 在知识库详情页顶部新增“知识向量”按钮, 跳转现有 `/knowledge-center/vectors` 并携带知识库上下文。
- [x] 移除原“知识向量”左侧菜单入口, 并在菜单归一化时清理旧内置节点。
- [x] 知识向量页读取入口上下文, 默认筛选当前知识库。
- [x] 知识向量页新增返回按钮: 有知识库上下文时返回详情页, 否则返回知识库列表。
- [x] Chunk 列表字段调整为 ID、内容、Chunk大小、所属知识库、所属文档、向量模型、向量维度、创建时间、更新时间。
- [x] 实体列表字段调整为 ID、实体名称、实体类型、属性、关联关系数、实体描述、所属知识库、所属文档、来源Chunk、向量模型、向量维度、创建时间、更新时间。
- [x] 关系列表字段调整为 ID、头实体、尾实体、关系类型、关系描述、所属知识库、所属文档、来源Chunk、向量模型、向量维度、创建时间、更新时间。
- [x] 去掉知识向量页相似度检索面板。
- [x] 去掉列表顶部批量重新向量化、批量删除、重建索引按钮。
- [x] 运行 `npm run typecheck` 验证类型通过。
- [x] 在关系后新增“可视化”Tab, 展示当前筛选范围内向量的 mock 二维散点图。
- [x] 可视化 Tab 支持点选详情、滚轮缩放、拖拽平移和重置视图。
- [x] 将向量可视化调研结论、交互规则和 mock 投影方式同步写入 specs。
- [x] 按 Milvus t-SNE 散点图风格优化可视化画布、网格、坐标轴和图例。
- [x] 增加“按类型着色 / 按查询着色”切换, 并基于 `vectorPreview` 计算 Top 8 相似点。
- [x] 增加可视化内全类型搜索定位, 支持 Chunk、实体、关系命中后作为 Query 展示相似点。
- [x] 增加降维算法下拉框（PCA、t-SNE、UMAP）, 并将着色方式切换改为下拉框。
- [x] 将知识向量页返回按钮调整为知识图谱内页同款样式, 去掉知识库/集合/模型/状态筛选, 并复用右上角搜索定位可视化散点。
- [x] 修复可视化缩放/平移时坐标网格与刻度不联动的问题, 并增加右侧健康度分析面板。
- [x] 将知识向量页顶部指标调整为向量分布、向量重复率、向量方差、聚类数量, 并为每项增加可点击叹号解释。

## 后续候选
- [ ] 接入真实后端时, 确认解析策略提交值与展示名是否需要拆分。
- [ ] 统一文档详情、知识库详情中的“解析器”文案是否升级为“解析策略”。
- [ ] 真实召回接口接入时, 确认链路步骤、耗时和命中指标由后端返回还是前端组装。
