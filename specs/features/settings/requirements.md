# Settings · Requirements

> 关注 **WHAT**: 模块解决什么业务问题、谁来用、达到什么程度才算"完成"。HOW 留给 [design.md](./design.md)。

## 概述 · Overview
平台级配置入口: 用户与角色、权限、审计日志、连接凭证、环境与功能开关、品牌定制。

## 目标用户 · Personas
| 角色 · Role | 场景 · Scenario | 期望产出 · Outcome |
|---|---|---|
| 运维管理员 | 给新成员开账号 / 分配角色 | 安全可审计的配置流程 |
| 数据负责人 | 查看审计日志, 定位异常操作 | 时间序检索 + 过滤 |
| 普通用户 | 切换语言 / 主题 / 偏好 | 即时生效, 持久化 |

## 用户故事 · User Stories
- **US-01** 作为运维管理员, 我希望在一处看到所有用户与其角色, 以便审计与调整。
- **US-02** 作为普通用户, 我希望切换语言 / 通知偏好, 立即生效, 持久化。
- **US-03 菜单自定义** 作为任意用户, 我希望在 `/settings/menu` 自定义左侧 Sidebar 树菜单的**顺序 / 嵌套 / 显隐 / 重命名**, 以便按个人习惯组织工作区入口。
- **US-04** 作为运维, 我希望审计日志可按时间 / 操作类型 / actor 过滤。
- **US-05 设置访问门禁** 作为工作区使用者, 我希望系统设置需要输入约定密码后才能查看, 以减少普通用户误入管理页面。

## 验收标准 · Acceptance Criteria (EARS)
- **AC-01** 当用户进入 `/settings` 时, 系统应当显示子页 sub-nav(Menu / Preferences / Users / Permissions / Audit / Flags), 默认进入 `Menu`。
- **AC-02 菜单自定义** 当用户在 `/settings/menu` 拖拽菜单节点时, 系统应当: ① 实时预览树结构变化; ② 保存后立即在左侧 Sidebar 反映; ③ **不**改变任何路由地址(对应 KG `AC-G-MENU-CUSTOMIZE`)。
- **AC-03 菜单内置项保护** 用户**不**能删除内置 feature 路由对应的菜单项, 只能**隐藏**; 隐藏后仍可通过 URL 直接访问对应路由。
- **AC-04 菜单自定义分组** 用户可创建"自定义分组"(`customGroup: true`) 作为父节点, 仅展示用, 不绑定路由。
- **AC-05 菜单重置** 提供"重置为默认结构"按钮, 二次确认后生效。
- **AC-06 菜单双语** 用户可为每个菜单节点分别填入 zh-CN / en-US 名称; 切换语言时同步显示对应语言。
- **AC-07** 当用户修改偏好(语言 / 通知) 时, 系统应当 optimistic 更新 UI 并持久化, 失败时回滚 + toast 提示。
- **AC-08** 当 `mockClient` 失败时, 各子页应显示工作区级错误态, 不卡死页面。
- **AC-09 设置访问门禁** 当用户通过 Sidebar 或直接 URL 进入任意 `/settings/*` 路由且尚未通过验证时, 系统应当隐藏设置内容并要求输入密码。
- **AC-10 密码来源** 设置访问密码只存放在公开静态文件 `public/settings-access.md` 中, TypeScript / TSX 与翻译文件不得包含密码明文。前端运行时读取并解析该文件。
- **AC-11 长期授权** 密码验证成功后, 系统应当将授权标记持久化到 `localStorage`; 刷新、关闭并重新打开浏览器后仍可访问设置页面。
- **AC-12 失败处理** 密码错误时系统应显示行内错误且不渲染设置内容; 密码文件加载或解析失败时应拒绝访问并显示可重试的错误状态。
- **AC-13 完整产品菜单** Sidebar 应展示产品矩阵、行业方案、数据平台产品域、AI 扩展和系统设置；旧缓存必须补齐新增内置路由并移除已废弃节点。

## 范围 · In Scope
- 用户偏好(语言 / 通知)
- **菜单自定义 (P1, 本期 spec 重点)**
- 用户与角色管理 (P1)
- 权限策略 (P1)
- 审计日志 (P1)
- 功能开关 (P2)
- 设置模块的前端密码门禁

## 非目标 · Out of Scope
- SSO / OAuth 接入(后端职责, 前端只做 UI)
- 安全鉴权或密码保密; `public/settings-access.md` 可被浏览器直接访问, 本功能只防止普通误入
- 企业品牌深度定制(LOGO / 域名)

## 依赖 · Dependencies
- 上游 · Upstream: 后端用户与权限服务 (mock)
- 下游 · Downstream: 全应用 (语言 / 主题 / 权限校验)
- 外部 · External: 待定 SSO IdP

## 风险与未决 · Risks & Open Questions
- ❓ 权限模型 (RBAC / ABAC) → 待 ADR
- ❓ 审计日志的保留与导出策略
- ❓ 跨子模块的开关是否在此集中管理
