# ADR-0011 · 安全审计引用来源域证据

## 状态 · Status

Accepted

## 决策 · Decision

数据访问、流转、交换、平台操作和任务执行的原始事件分别由产生事实的数据资产、数据集成、设置、调度、开发或运维域拥有。数据安全域建立统一证据目录，以稳定事件 ID 引用来源记录并登记来源、时间范围、完整性校验、采集状态和证据缺口；审计分析与报告形成不可覆盖快照，但不复制或修改来源事实。这样可以避免多个 feature 各自维护冲突的“原始审计日志”，同时保留跨域审计和举证能力；SQLite 原型仅以脱敏 mock 事件和校验值模拟该关系。
